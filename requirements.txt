# Core agent framework
langgraph>=1.2.11,<2.0
langchain>=1.3.15,<2.0
langchain-core>=1.5.4,<2.0

# LLM provider (live mode) - free tier at console.groq.com, no credit card
langchain-groq>=1.1.3,<2.0

# Embeddings + vector store (live mode RAG) - both run fully locally, no API cost
langchain-huggingface>=1.2.2,<2.0
sentence-transformers>=3.0.0
langchain-chroma>=1.1.0,<2.0
chromadb>=1.5.9,<2.0

# Mock mode (offline fallback - keyword/TF-IDF search, rule-based scoring)
scikit-learn>=1.8.0,<2.0

# Data + config
faker>=40.0.0
python-dotenv>=1.2.2
pydantic>=2.13.0,<3.0

# Interfaces
rich>=15.0.0
streamlit>=1.61.0
